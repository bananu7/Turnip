#This is Turnip.

Turnip is a fully pure Lua VM writen in Haskell. It started as a compiler project (which got stuck around the AST phase) by a different person.

My personal goal is providing a way to use Lua in Haskell without using `IO`. That's all.